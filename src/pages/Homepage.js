import React from 'react';
import './Homepage.css';

const Homepage = () => {
    return (
        <div>
            <h1>Homepage</h1>
            <a href="/settings" className="nav-link">Settings</a>
        </div>
    );
};

export default Homepage;